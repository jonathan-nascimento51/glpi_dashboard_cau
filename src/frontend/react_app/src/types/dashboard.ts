export interface DashboardStats {
  new: number
  pending: number
  progress: number
  resolved: number
}

export interface TicketMetrics {
  total: number
  opened: number
  closed: number
}
