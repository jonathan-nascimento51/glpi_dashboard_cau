{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ticket",
  "type": "object",
  "properties": {
    "id": {"type": "integer"},
    "status": {"type": "string"},
    "group": {"type": "string"},
    "date_creation": {"type": "string", "format": "date-time"},
    "assigned_to": {"type": "string"},
    "name": {"type": "string"}
  },
  "required": ["id", "status", "group", "date_creation", "assigned_to", "name"]
}