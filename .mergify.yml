 pull_request_rules:
   - name: Rebase automático e merge com checks verdes
     conditions:
       - base=main
       - "status-success=ci (Mock)"   # aspas evitam erro de parsing
     actions:
       rebase: {}
       merge:
         method: squash
         merge_bot_account: "mergify[bot]"
